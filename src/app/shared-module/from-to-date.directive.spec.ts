import { FromToDateDirective } from './from-to-date.directive';

describe('FromToDateDirective', () => {
  it('should create an instance', () => {
    const directive = new FromToDateDirective();
    expect(directive).toBeTruthy();
  });
});
